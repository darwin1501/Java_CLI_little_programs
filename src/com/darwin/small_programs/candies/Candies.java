package com.darwin.small_programs.candies;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Candies {
    public void eat(){
        Integer[] sugarOnCandies = { 33, 20, 12, 19, 29 };
        int sugarLimit = 33;
        int sugarConsumed = 0;
//         candies consumed before reaching the limit
        ArrayList<Integer> candiesConsumed = new ArrayList<>();

        // sort the sugar on each candy
        // add each sugar on candy
        // find the index of candies
        // if sugar limit reach then stop adding

//        for (int i = 0; i < sugarOnCandies.length ; i++) {
//
//        }
        System.out.println(candiesConsumed);
    }
}
